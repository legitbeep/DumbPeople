import { Box } from "@chakra-ui/react";

import HeroSection from "components/HeroSection";

const Home = () => {
  return (
    <Box mb={8} w="full">
      <HeroSection />
    </Box>
  );
};

export default Home;
