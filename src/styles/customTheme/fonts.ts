import { DeepPartial, Theme } from "@chakra-ui/react";

const fonts = {
    body : "Raleway, sans-serif",
    heading : "AkiraBold, Syncopate, sans-serif",
    outlined : "AkiraOutline, sans-serif"
};

export default fonts;